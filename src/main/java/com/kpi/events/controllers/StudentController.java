package com.kpi.events.controllers;
/*
import com.kpi.events.model.Student;
import com.kpi.events.model.StudentIn;
import com.kpi.events.model.dto.StudentDto;
import com.kpi.events.services.StudentService;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.web.bind.annotation.*;

import java.util.List;

@RestController
@RequestMapping("/api")
public class StudentController {

    @Autowired
    private StudentService service;

    @GetMapping(path = "/students")
    public List<Student> getStudents(@RequestParam(required = false, defaultValue = "10") int limit,
                                     @RequestParam(required = false, defaultValue = "0") int page) {
        return service.findAll(limit, page);
    }

    @PostMapping(path = "/login_student")
    public StudentDto loginStudent(@RequestBody StudentIn student) {
        return service.loginStudent(student);
    }
}
*/